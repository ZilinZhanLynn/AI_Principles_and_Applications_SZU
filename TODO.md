# 课程网站维护待办

供教师与网站维护者使用，不作为学生课程内容。

## 暂缓：接入 AI 助教反馈

记录日期：2026-09-17。教师计划之后注册模型 API 服务，再确认接入；本次不注册、不购买、不调用服务。

- [ ] 教师确定服务商、可用地区、预算并开通 API；不要在对话中粘贴密钥。
- [ ] 增加后端或无服务器接口。GitHub Pages 继续承载静态网页；密钥只保存在服务端，不能放进 HTML、前端 JavaScript 或公开仓库。
- [ ] 优先试点第 2 课“我的判断与依据”：按 PEAS、环境判断、假设和理由提供形成性反馈。输出“已理解的部分 / 需要补充的部分 / 下一步提示”，不直接认定为正式成绩。
- [ ] 为每个任务提供教师确认的评价标准；接受假设合理、论据充分的不同答案。模型无法判断时明确说明，不强行给分。
- [ ] 算法计算优先使用确定性程序校验；模型仅辅助解释。用匿名样例与教师判断对照，验证反馈质量后再开放。
- [ ] 学生主动点击后才发送答案；明确告知发送内容、接收服务和保存策略，不自动上传输入，不要求姓名或学号。
- [ ] 加入访问控制、限流、费用上限、超时与失败提示；不把开放代理暴露给任意访问者。
- [ ] 防范答案中的提示注入；不把学生输入当作系统指令。反馈以安全文本渲染。
- [ ] 保留现有提示与自查解释，模型不可用时仍能完成课堂任务。

当前状态：未接入 AI 批改；输入只在当前页面中临时保留，刷新即清空。

## 本次已完成

- [x] 第 2 课加入四组前沿选读，分别连接 PEAS、可观察性与动态环境、内部状态、绩效度量。
- [x] 标注原始来源、发布日期、指定选读片段、课程解读与边界；全部自愿选读、不纳入考试。
