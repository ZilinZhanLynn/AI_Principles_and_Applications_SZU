'use strict';
// Original course explanations. Examples are models, not claims about live tools.
(() => {
  const n=Number(new URLSearchParams(location.search).get('n'));
  if(![2,3].includes(n)||!document.getElementById('literature-lab'))return;
  const by=id=>document.getElementById(id);
  const table=(heads,rows)=>`<div class="foundation-table" role="region" aria-label="${heads.join('、')}对照表" tabindex="0"><table><thead><tr>${heads.map(h=>`<th scope="col">${h}</th>`).join('')}</tr></thead><tbody>${rows.map(row=>`<tr>${row.map((v,i)=>i?`<td>${v}</td>`:`<th scope="row">${v}</th>`).join('')}</tr>`).join('')}</tbody></table></div>`;
  const more=(title,body)=>`<details class="support foundation-detail"><summary>${title}</summary>${body}</details>`;
  const section=(id,title,body)=>{const e=document.createElement('section');e.id=id;e.className='lesson-section foundations';e.innerHTML=`<h2>${title}</h2>${body}`;return e;};
  const link=(url,name)=>`<a href="${url}" target="_blank" rel="noopener noreferrer">${name} ↗</a>`;
  const insertNav=(id,title,before)=>{const a=document.createElement('a');a.href='#'+id;a.textContent=title;const anchor=document.querySelector(`.lesson-nav a[href="#${before}"]`);if(anchor)anchor.before(a);else document.querySelector('.lesson-nav nav').append(a);};
  if(n===2)agent();else search();
  if(location.hash)requestAnimationFrame(()=>by(location.hash.slice(1))?.scrollIntoView());

  function agent(){
    const bridge=section('agent-economics','设计一个 AI 助手，到底在设计什么？',`<p>不只是写一句提示词。我们还要明确：怎样才算做好、信息从哪里来、允许做哪些事。教材第 2 章用 <strong>PEAS</strong> 把这些问题分开描述。</p><h3>与经济学中的理性选择对照</h3>${table(['问题','经济学中的一种分析方式','智能体设计'],[['谁作选择','具有偏好、面对约束的决策者','接收感知、执行行动的系统'],['怎样评价','用偏好或效用描述结果的好坏','先约定外部性能度量 P'],['根据什么决定','已有信息、信念与可行选择','感知序列、先验知识与可用动作'],['不确定时怎么办','比较可行行动的期望效用','选择预期表现更好的行动']])}<p class="note">这是帮助理解的类比，不是两个领域的定义完全相同。外部性能度量不等于内部效用函数；“理性”也不等于全知、结果总成功，或系统必然公平。</p>`);
    by('overview').after(bridge);insertNav('agent-economics','智能体与理性选择','warmup');
    by('warmup').insertAdjacentHTML('beforeend',`<details class="support"><summary>热身小判断：哪些可以作为智能体来分析？</summary><label for="agent-example">选择一个例子</label><select id="agent-example"></select><label for="agent-verdict">你的判断</label><select id="agent-verdict"><option value="">先选择</option><option value="yes">可以</option><option value="no">不适合这样建模</option><option value="depends">取决于系统边界</option></select><label for="agent-reason">依据：它接收什么信息，通过什么接口行动？</label><textarea id="agent-reason" rows="2" placeholder="仅临时记录，不上传、不评分。"></textarea><button id="agent-judge" type="button" class="button secondary">对照分析思路</button><p id="agent-judge-result" role="status"></p></details>`);
    const examples=[['自动驾驶汽车','可以把车辆控制系统作为智能体：摄像头等输入，经转向、制动等装置行动。具体能力取决于系统边界。'],['医生远程操控的手术机器人','需先说明分析对象。医生＋设备可视为感知—行动系统；若只看纯遥控机械执行端，不能据此断言它会自主决策。“智能体”与“自主程度高”不是同一个概念。'],['恒温器','可以作为简单反射智能体：读取温度，按条件控制加热接口，不需要大模型或身体外形。'],['猫','可以：通过视觉、听觉等感知，以身体动作影响环境。智能体不只指计算机程序。'],['AlphaGo','可以：接收棋局信息并选择落子。学习、搜索与预先编写的程序可以同时存在。'],['网页爬虫','可以：读取页面与链接，并按规则请求下一页。是否使用机器学习并不是判断的必要条件。'],['传统风车','通常不作为本课的信息处理型智能体例子：主要是风能驱动机械转动。若增加测风、控制与目标，建模边界就变了。不要把这一课堂约定当作所有领域的唯一分类。'],['商场自动门','可以作为简单反射智能体：感知触发信号，经电机开关门；具体规则还应考虑安全条件。']];
    by('agent-example').innerHTML=examples.map(([name],i)=>`<option value="${i}">${name}</option>`).join('');
    by('agent-judge').onclick=()=>{by('agent-judge-result').textContent=!by('agent-verdict').value?'请先选择判断，再说明你划定的系统边界。':examples[Number(by('agent-example').value)][1]+' 本环节提供参考分析，不自动给分。';};
    by('agent-example').onchange=()=>{by('agent-verdict').value='';by('agent-reason').value='';by('agent-judge-result').textContent='换了例子，请重新判断。';};
    const foundation=section('agent-foundations','感知、决策、行动：把闭环说清楚',`
      <div class="foundation-loop" aria-label="智能体交互闭环"><span>环境与材料</span><b>→ 感知</b><span>内部状态与决策</span><b>→ 行动</b><span>新的观察结果</span><b>↩ 更新状态</b></div>
      <p>对文献助手，读取原文是一次工具行动，工具返回的文本是新的感知。文字输出也可以是行动；但没有检索工具的聊天模型不能声称已查过外部文献库。</p>
      <h3>PEAS：先定义任务，再决定实现</h3><div class="foundation-cards">
      <article><h4>P · Performance measure<br>性能度量</h4><p>评价完成得好不好的标准：相关性、摘要准确性、证据可追溯性与成本。应在设计时明确，可评价整个过程和结果，不是只在事后才考虑。</p></article>
      <article><h4>E · Environment<br>环境</h4><p>研究问题、授权文献库、原文、用户反馈与工具限制。只用固定材料，和搜索持续更新的外部库，是不同的任务环境。</p></article>
      <article><h4>A · Actuators<br>执行器／行动接口</h4><p>查询工具、打开原文的接口、证据表输出接口。接口使系统能够查询、阅读和写出结果；“写出准确综述”是目标，不是执行器。</p></article>
      <article><h4>S · Sensors<br>传感器／感知接口</h4><p>接收用户问题、检索返回值和原文文本的接口。材料内容是感知数据；接收到一段摘要，不等于知道全文或所有相关研究。</p></article></div>
      ${more('教材迁移：出租车与购物助手的 PEAS',table(['维度','自动驾驶出租车','购物助手'],[['P','安全、守规、舒适、时间与费用','满足用户需求、价格与信息准确性；平台佣金可能与用户利益冲突'],['E','道路、交通参与者、天气、乘客','商品库、商家、用户需求与交易规则'],['A','转向、制动、驱动和显示装置','查询接口、列表输出接口；下单需要额外授权'],['S','摄像头、定位与其他传感器','接收查询、商品数据和用户反馈的接口']])+'<p>课堂实操仍使用文献助手。这两个例子用于检验同一套框架能否迁移，不增加新项目。</p>')}
      <h3>当前感知不等于感知序列</h3><p class="foundation-formula">f : P* → A</p><p>智能体函数把<strong>感知序列</strong>映射为行动。P 是可能感知的集合，P* 是有限感知序列的集合，A 是行动集合；这里的 P 不要与 PEAS 中的性能度量混淆。程序是这个抽象函数在具体设备上的实现。</p><p>“刚收到材料 A 的摘要”是当前感知；“用户提出问题 → 读到摘要 → 获取原文 → 收到用户修正”是感知序列。历史不同，合理动作<strong>可能</strong>不同，但不要求每两段不同历史都产生不同动作。</p>
      ${more('理性、信息价值与成本',`<p>理性是在已有感知和知识下，选择使期望性能更好的行动，不保证每次结果成功。</p><p>文献助手是否值得再查一次原文？要比较信息可能带来的决策改进与时间、费用等成本。不是所有额外查询都值得做；感知也不必总是被动的。</p><p>教学计算：额外核验预计可避免 8 个单位的错误损失，成本为 3，在其他条件相同且估计可信时，预期净收益为 5。把成本改为 10，选择可能改变。涉及必须核验的硬性要求时，不能用省时得分抵消。</p>`)}
      <h3>任务环境：六个维度，先写假设</h3>${table(['维度','要问的问题','文献助手中的条件判断'],[['完全／部分可观察','当前信息是否足以确定任务相关状态？','只给摘要时，无法确认全文证据，通常是部分可观察。'],['单／多智能体','其他决策者的行动是否影响结果？','静态库中的单个整理程序可简化为单智能体；多个助手共享任务与结果时需考虑协作。'],['确定／随机','同一状态与动作的后果是否唯一？','本页固定规则是确定的；真实工具失败或随机生成需另行建模。未知规则不等于随机。'],['片段式／序贯','本次选择是否影响后续选择？','先筛选再核验会影响后续证据集合，属于序贯任务。'],['静态／动态／半动态','思考期间环境或评价是否变化？','固定库可视为静态；检索期间相关信息更新时可视为动态；库不变但时间得分变化是半动态例子。'],['离散／连续','状态、时间、观察与动作怎样取值？','本页有限材料与按钮动作采用离散模型；真实响应时间可连续建模。离散不等于一定有限。']])}<p class="note">补充：已知／未知描述智能体是否知道转移与观察规律，与完全／部分可观察是不同问题。环境分类取决于具体任务与建模假设，不给某个产品永久贴标签。</p>
      <h3>六个视角理解智能体架构</h3><p>“智能体＝架构＋程序”强调设备和程序配合。下面把查表这个概念起点、四类基本决策结构和学习机制放在一起比较；它们不是六个互斥的等级。</p>
      ${table(['结构／机制','如何作决定','文献任务中的简化例子'],[['表驱动','为每段感知序列查行动表','预先列举“收到什么→做什么”；历史组合太多，难以穷举。'],['简单反射','只看当前感知，匹配条件—动作规则','若当前返回“未找到”，则提示无结果；不记得以前已试过哪些查询。'],['基于模型的反射','结合历史、动作与模型更新内部状态，再应用规则','记录已经核验哪些文献，避免重复操作；状态估计也可能出错。'],['基于目标','比较行动及其后果，寻找达到目标的方案','计划怎样得到两条相关且核验通过的证据；下一课展开。'],['基于效用','在可行结果之间比较好坏；不确定时考虑期望效用','在质量要求满足的前提下权衡时间与覆盖面；不等于必然找到全局最优。'],['学习机制','根据经验改变决策或模型','根据有依据的反馈改进筛选；仅保存对话不等于已发生学习。']])}
      ${more('两处容易混淆的细节',`<p>简单反射策略可以在部分可观察环境中运行；只有当前感知足以决定合适动作时，它才可能表现良好。不能据此要求所有任务必须完全可观察、静态。</p><p>查表规模取决于感知的可能数 m：长度 n 的序列有 mⁿ 种。若两格吸尘器每次观察“位置＋当前脏／净”，则 m＝4，对应 4ⁿ，而不是固定为 2ⁿ。</p><p>学习智能体常用四个组成部分描述：性能元件选行动、学习元件作改进、评估者给反馈、问题生成器提出有信息价值的探索。学习可以附加在不同决策结构上。</p>`)}
      <p class="foundation-source">理论主线：原课件 02、AIMA 第 2 章。辅助阅读：${link('https://inst.eecs.berkeley.edu/~cs188/textbook/search/agents.html','CS188 · Agents')}。补充说明不新增考核要求。</p>`);
    by('warmup').after(foundation);insertNav('agent-foundations','PEAS、环境与架构','literature-lab');
    const modern=section('agent-modern','选读：从教材智能体到 LLM Agent',`<p class="muted">只需理解连接与边界，不要求搭平台、付费或交报告。以下是课程解读，不把现代系统直接等同于教材某一类架构。</p>
      ${more('LLM、工具与 RAG：各自负责什么？',`<p>在一些系统里，LLM 参与选择下一步；工具接口执行查询，返回结果作为观察，外部记录保存任务进度。工程语境常把按固定路径执行的工作流与由模型动态选择步骤的 Agent 区分开。</p><p>RAG 是检索信息后辅助生成的方式，不自动等同于长期记忆，更不能保证引用正确。纯模型、聊天产品和带工具的 Agent 系统也不是同一个层级。</p><p>带着问题读：你的文献助手缺的是可用工具、可靠材料，还是明确的评价标准？</p><p>${link('https://www.anthropic.com/engineering/building-effective-agents','Building effective agents · 2024-12-19')}：先读 What are agents 与 When (and when not) to use agents。工具说明可能变化，阅读重点是设计取舍。</p>`)}
      ${more('ReAct：把推理与行动交替起来',`<p>ReAct 探索交替产生推理步骤和动作，再利用观察继续任务。例如：发现摘要缺方法 → 查询原文 → 依据返回内容修正总结。</p><p>它不是教材“基于模型的反射智能体”的同义词，也不是 A*；输出总结本身仍可以是行动。要看任务是否需要查询、是否真的执行了工具，而不是只看模型有没有写“我准备搜索”。</p><p>${link('https://arxiv.org/abs/2210.03629','ReAct · 2022 预印本 / ICLR 2023')}：先看摘要与框架图。问题：如果工具失败，流程怎样停止或请求帮助？</p>`)}
      <p>多智能体分工与评估：继续阅读下方“课件与选读”中的研究助手、绩效度量，以及教材第 7 章、第 18 章入口。两个助手互相赞同不等于独立核验；研究空白也只能作为待查证的候选。</p>`);
    by('materials').before(modern);insertNav('agent-modern','LLM Agent 选读','materials');
  }

  function search(){
    const bridge=section('search-why','为什么大模型时代还要学习搜索？',`<p>上一讲问“怎样才算做好”，这一讲问“从现在出发，哪些步骤能完成目标”。搜索让我们把候选方案、合法动作、停止条件和资源成本写清楚。</p><p>它提供一套可执行、可检查的推理框架。不是所有 AI 都等于经典搜索：参数优化、文本采样与状态空间搜索有联系，但对象、规则和保证不同。问题求解也能考虑最低路径成本，并非只能判断到没到目标。</p><p class="note">本课先研究固定、可观察、动作结果确定且规则已知的简化任务。真实文献综述的质量判断与信息缺失，需要额外建模，不能由一个搜索算法自动解决。</p>`);by('overview').after(bridge);insertNav('search-why','为什么学搜索','warmup');
    const foundation=section('search-foundations','把任务写成计算机能执行的定义',`
      <h3>五项定义：从自然语言到搜索问题</h3>${table(['要素','文献库课堂模型'],[['状态空间','可区分的（当前材料、已读集合、已核验集合、已纳入集合）组合；只保留合法组合。'],['初始状态','当前材料为 A；已读、已核验、已纳入集合都为空。'],['合法动作与转移','打开、阅读、核验、纳入。给定状态，检查前置条件并计算后继。'],['目标检验','至少纳入要求数量，且每条都相关、核验通过。'],['路径代价','本例累加阅读、核验、纳入的教学耗时；单位保持一致。']])}<p>教材也可能把“动作集合”和“转移模型”分开列。要点是把信息写全，不是死记项目数量。若只求任意可行解，可以暂不比较成本；要讨论最优解，就必须说明“好”的标准。</p><p class="foundation-formula">Succ(s) = { (a, Result(s,a)) | a ∈ Actions(s) }</p><p>后继函数返回合法动作及相应后继。尚未核验 A 时，“纳入 A”不在合法动作中。问题定义可以按需生成后继，不要求先画完整张图。</p>
      ${more('状态表示：原子、因子化、结构化',table(['表示','怎样看状态','文献模型'],[['原子','把状态作为不可分解的标识处理','把完整任务配置编码为编号 s₁、s₂；算法不读取内部字段。'],['因子化','用一组变量描述状态','当前材料、各文献的已读／核验／纳入标志。'],['结构化','表示对象、属性与关系','论文、结论、证据片段，以及“结论来自某片段”的关系。']])+'<p>这是表示方式，不是能力等级；用数字对表示水量具有变量结构，但搜索算法也可以把整个数字对作为原子键。仅记录“已总结 3 篇”往往不足以区分总结的是哪几篇。</p>')}
      <h3>状态 ≠ 搜索节点</h3><p>状态回答“现在是什么情况”；搜索节点还记录“怎么到这里”：父节点、所用动作、深度和累计代价 g(n)。不同路径可以到达同一状态，但对应不同节点。</p><div class="foundation-cards"><article><h4>状态图：问题的结构</h4><p>同一状态对应一个顶点，动作是边。它可能很大，不一定实际存储完整图。</p></article><article><h4>搜索树：探索的路径</h4><p>从初始节点逐步展开；不同路径可能多次出现同一个状态。节点数不等于不同状态数。</p></article></div><p>例如：先核验 A 再核验 B，或反过来，最后都打开 A。若三个集合内容相同，且历史顺序不影响后续动作，那么它们可以是同一状态；两条搜索路径仍各有自己的节点记录。集合比较应忽略元素写入顺序。</p>
      <h3>边界表、树搜索与图搜索</h3><div class="foundation-loop"><span>初始节点入表</span><b>→</b><span>按策略取出</span><b>→</b><span>检查目标</span><b>→</b><span>扩展合法后继</span><b>↩ 更新边界表</b></div><p>边界表（frontier）保存已生成、待处理的候选节点。第 4 讲用队列和栈改变取出顺序；具体算法的目标检验时机、去重和更新规则要明确。</p>${table(['方法','怎样处理重复状态','代价与注意点'],[['基础树搜索','按路径展开，不做全局重复检测','可能反复生成同一状态；常见变体还会检查当前路径的环。依然需要边界表等内存。'],['图搜索','记录已到达／已探索状态，按算法规则处理重复','可以减少重复，但需要额外记录。UCS、A* 不能一律“见过就永远丢弃”：更低成本路径可能需要更新或重新打开。']])}<p>只记录“当前文献”时，沿 A→B→A 的链接可能重复探索。加入历史字段是否必要，取决于任务：找一篇目标和收集多篇证据，不能使用同一套过度简化的状态。</p>
      ${more('搜索方法在课程中的位置',table(['类别','信息与规则','课程位置'],[['无信息搜索','使用问题定义、目标检验与必要的路径成本，但没有额外的剩余代价启发式','第 4 讲 BFS/DFS；第 5 讲 UCS；深度受限与迭代加深作补充'],['有信息搜索','用启发式估计引导候选排序','第 6 讲贪心、A*；第 7 讲比较与选型'],['对抗搜索','考虑对手的行动与结果回传','第 8 讲 Minimax；第 9 讲 Alpha-Beta'],['局部搜索','在候选解及其邻域中改进解','只作教材定位，不新增教学或考核要求']])+'<p>“无信息”不等于没有动作模型、目标或地图，也不保证每种算法都完备。MCTS 不列入本课程教学任务。</p>')}
      <h3>怎样评价搜索方法？</h3>${table(['指标','要回答的问题'],[['完备性','在约定条件下，有解时是否保证找到？'],['最优性','在约定条件下，是否保证返回最低代价解？'],['时间复杂度','需要生成或扩展多少节点等计算工作？'],['空间复杂度','同时需要保存多少信息？']])}<p>前两项关心保证，后两项关心资源。保证与资源经常存在取舍，但不能仅凭一次实验断言某算法总更快；比较时要写明条件与计数口径。</p>
      ${more('教材迁移：八数码，用同一框架再看一次',`<div class="puzzle-example" role="img" aria-label="初始棋盘：第一行1 2 3，第二行4 5 6，第三行空格7 8。目标：第一行1 2 3，第二行4 5 6，第三行7 8空格。"><pre>初始             目标\n1 2 3            1 2 3\n4 5 6     →      4 5 6\n□ 7 8            7 8 □</pre></div><p>状态是棋盘排列；初始如图；合法动作是把空格与相邻方块交换；目标检验为排列一致；每步成本为 1。先自己写出两个合法动作，再观察：将空格向右移动两次即可完成。</p><p>9! 是所有排列数；从某个合法初始排列经滑动可达其中一半，即 181,440 个。不是任意初始排列都能到达指定目标。</p>`)}
      <p class="foundation-source">理论主线：原课件 03、AIMA 第 3 章。辅助阅读：${link('https://inst.eecs.berkeley.edu/~cs188/textbook/search/state.html','CS188 · State Spaces and Search Problems')}。</p>`);
    by('warmup').after(foundation);insertNav('search-foundations','问题定义与搜索框架','literature-lab');
    const challenge=section('literature-state-challenge','状态建模挑战：哪些信息真的需要记录？',`<p>从下方两种任务中选一种，先看假设，再选状态字段。状态不是“把所有资料都存进去”，而是保留决定后继动作和目标判断所需的信息。</p><label for="state-scenario">选择任务</label><select id="state-scenario"><option value="evidence">固定库：收集核验通过的证据</option><option value="review">拓展：组织文献综述</option></select><p id="state-assumptions" class="note"></p><fieldset><legend>勾选需要随任务更新的状态信息</legend><div id="state-fields" class="state-options"></div></fieldset><button id="state-fields-check" type="button" class="button secondary">检查字段选择</button><p id="state-fields-result" role="status"></p><h3>用反例检查你的表示</h3><p id="state-counterexample"></p><label for="state-merge">能否合并为同一个状态？</label><select id="state-merge"><option value="">先判断</option><option value="no">不能，后续动作或完成判断不同</option><option value="yes">可以，只看当前阶段就够了</option></select><label for="state-why">写出你的依据</label><textarea id="state-why" rows="3" placeholder="仅临时记录，不上传、不评分。"></textarea><button id="state-merge-check" type="button" class="button secondary">对照反例解释</button><p id="state-merge-result" role="status"></p>`);by('literature-lab').after(challenge);insertNav('literature-state-challenge','状态建模挑战','activity');
    stateChallenge();
    const modern=section('search-modern','选读：搜索与大模型，联系在哪里？',`<p class="muted">读摘要、看框架、问一个问题即可。不新增考试要求，也不把这些材料当作真实文献综述的证据。</p>
      ${more('解码：序列概率高，是否就是真实？',`<p>自回归语言模型逐步产生词元。贪心解码选择当前最高概率词元；束搜索保留有限个候选序列；Top-k、Top-p 则限制候选后采样，不能直接等同于 BFS 或 DFS。</p><p>局部最高概率的选择不保证整个序列概率最大；更高概率也不保证事实正确。搜索是理解某些解码策略的视角，不是所有模型工作的完整解释。</p><p>${link('https://huggingface.co/docs/transformers/generation_strategies','Transformers 官方文档 · 生成策略')}。想一想：文献摘要很流畅，为什么仍要逐条核验原文？</p>`)}
      ${more('CoT 与 ToT：一步步写，还是显式探索多条候选？',`<p>CoT 提示在原论文所测试的任务与模型中展示了中间步骤提示的价值。它不自动维护搜索树，也不保证中间文字忠实反映内部计算；“写得更长”不等于“更正确”。</p><p>ToT 将中间想法作为候选，结合评估和搜索探索不同路径。它与启发式搜索有联系，但不等于 A*，也不自动满足启发式可采纳性或最优性保证。</p><p>${link('https://arxiv.org/abs/2201.11903','CoT · Wei 等，2022')}；${link('https://arxiv.org/abs/2305.10601','Tree of Thoughts · Yao 等，2023')}。</p><p>带着问题读：文献助手生成两份相冲突的摘要时，“选择听起来更好的一份”和“查原文验证”有什么区别？</p>`)}
      ${more('推理时计算：花更多时间，一定更好吗？',`<p>增加推理时计算可以用于较长生成、多次采样、验证或搜索，具体方式取决于系统，不能仅凭等待时间断言它在做 DFS 或 MCTS。</p><p>${link('https://arxiv.org/abs/2501.12948','DeepSeek-R1 技术报告 · 2025')}：先看摘要中的强化学习训练思路。训练计算与推理时计算是不同阶段；更多预算不保证每个任务都提升，也不能据此断言推理模型一定不擅长闲聊。</p><p>课堂问题：若多核验一次成本翻倍，你会用什么证据判断它是否值得？</p>`)}
      ${more('Agentic Search：查资料不等于状态空间搜索',`<p>文献助手可以循环“提出查询 → 读取材料 → 发现缺口 → 改写查询 → 核验证据”。这里的检索常涉及索引、相关性排序与数据库接口；经典搜索则按明确的状态、动作和转移规则探索。</p><p>两者可以组合，但不能仅凭用了关键词就称为无信息搜索，或凭用了大模型就称为 A*。先说明要搜索的对象、候选如何生成、怎样评价与停止。</p><p>${link('https://www.anthropic.com/engineering/multi-agent-research-system','多智能体研究助手 · 2025-06-13')}。思考：当前材料没有覆盖某一方向，足以证明这个领域存在研究空白吗？不够，还需要更广泛、可复核的检索。</p>`)}
      <p class="foundation-source">以上为课程解读；原始资料核验日期：2026-09-18。未将 MCTS 或 AlphaGeometry 的技术细节加入本课。</p>`);by('materials').before(modern);insertNav('search-modern','搜索与 LLM 选读','materials');
  }
  function stateChallenge(){
    const scenarios={
      evidence:{assumptions:'库、主题、相关性规则和目标数量固定；动作是否可做取决于当前材料及阅读、核验、纳入状态。无总预算硬约束。',fields:[['current','当前打开的材料'],['read','已读过哪些文献'],['verified','核验过哪些文献'],['included','纳入了哪些文献'],['interest','固定研究主题与偏好（每步重复存入状态）'],['avatar','学生头像颜色'],['identity','真实姓名与学号']],required:['current','read','verified','included'],pair:'甲、乙都打开 A。甲已核验 A，乙只读过 A 的摘要。若 A 相关，两者现在都能执行“纳入 A”吗？',answer:'不能合并。甲可以纳入，乙必须先核验；仅记录当前材料会丢失决定合法动作的信息。'},
      review:{assumptions:'拓展纸面模型：主题固定为“双碳目标对企业创新的影响”。固定目标：覆盖两个预设方向，完成证据表与综述草稿。摘要、覆盖标记和草稿进度分别存储并参与后续动作判断；本模型不维护当前打开的材料。没有真实论文或联网检索。',fields:[['summaries','已总结的文献及观点、方法、结论'],['evidence','结论的来源位置与核验状态'],['coverage','两个预设方向的覆盖标记'],['draft','综述草稿的完成进度'],['interest','固定研究主题与偏好（每步重复存入状态）'],['avatar','学生头像颜色'],['identity','真实姓名与学号']],required:['summaries','evidence','coverage','draft'],pair:'两个助手都在“写初稿”。甲已整理 15 篇，乙只整理 3 篇，而且证据与覆盖情况不同。只记录“写初稿”能判断是否还需补充材料吗？',answer:'不能合并。阶段名称掩盖了证据与覆盖差异；篇数也不是质量保证。若覆盖情况可从其他字段完全推导，可以不重复存储，但需要说明新的表示与计算规则。'}
    };
    const selected=()=>scenarios[by('state-scenario').value];
    const render=()=>{const s=selected();by('state-assumptions').textContent=s.assumptions;by('state-fields').innerHTML=s.fields.map(([id,label])=>`<label><input type="checkbox" value="${id}"> ${label}</label>`).join('');by('state-counterexample').textContent=s.pair;by('state-fields-result').textContent='先依据假设选择字段。';by('state-merge').value='';by('state-why').value='';by('state-merge-result').textContent='';};
    by('state-scenario').onchange=render;
    by('state-fields-check').onclick=()=>{const s=selected(),chosen=[...by('state-fields').querySelectorAll('input:checked')].map(e=>e.value),missing=s.required.filter(id=>!chosen.includes(id)),extra=chosen.filter(id=>!s.required.includes(id)),name=id=>s.fields.find(f=>f[0]===id)[1];by('state-fields-result').textContent=(missing.length?'在当前表示中还缺：'+missing.map(name).join('、')+'。':'关键字段齐全。')+(extra.length?'不必作为变化状态重复记录：'+extra.map(name).join('、')+'。':'')+' 这是给定假设下的字段检查，不是 AI 评分；等价表示可以说明理由后讨论。';};
    by('state-merge-check').onclick=()=>{by('state-merge-result').textContent=by('state-merge').value?selected().answer:'请先作判断并写一句依据。';};render();
  }
})();
